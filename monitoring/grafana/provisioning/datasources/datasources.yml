apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus.talkertive.local:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: http://loki.talkertive.local:3100
