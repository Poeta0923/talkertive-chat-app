global:
  scrape_interval: 15s

scrape_configs:
  - job_name: talkertive-backend
    metrics_path: /metrics
    static_configs:
      # ECS Service Connect FQDN — Cloud Map 네임스페이스 talkertive.local
      - targets: ['backend.talkertive.local:8000']
